# 런타임 산출물 — 스크립트 실행 중 생성/갱신되는 파일 (스크립트 저장소에는 커밋하지 않음)

# 스크래퍼 진행 상태(재개용). 실행 시 자동 생성/갱신됨.
checkpoint.json
checkpoint.json.bak

# 생성 매니페스트(다운로드 대상 목록). build_manifest*.js / collect_catalog.js 산출물.
manifest2.json
manifest3.json
manifest_*.json
*_checkpoint.json
remaining2.txt
renames3.json
catalog.csv

# 백그라운드 실행 출력
*.nohup

# 로그
progress.log
incremental.log
done2.log
nohup.out
oversized.log
unavailable*.log
*.log

# 수집된 실제 데이터(이 저장소가 아니라 haechyaning-commits/data 로 감)
자체감사결과/
자체감사파일2/
